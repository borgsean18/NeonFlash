using UnityEngine;

namespace Camera
{
    public class CustomCamera : MonoBehaviour
    {
        // Exposed Variables
        [Header("Target")]
        [SerializeField] private Transform targetTransform;
    }
}
