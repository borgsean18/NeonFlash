using UnityEngine;

namespace Movement
{
    public class Movement : MonoBehaviour
    {
        // Exposed Vars
        [Header("Generic Values")]
        [SerializeField] protected float maxSpeed;
    }
}
